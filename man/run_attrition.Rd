% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/module_attrition.R
\name{run_attrition}
\alias{run_attrition}
\title{Module 2 - Cohort Attrition Audit}
\usage{
run_attrition(
  con,
  cdm_schema,
  cohort_table,
  cohort_id,
  obs_window = c(-365, 0),
  vocab_schema = "vocab",
  obs_warn_pct = 70,
  obs_fail_pct = 50,
  attrition_warn_threshold = 0.5
)
}
\arguments{
\item{con}{A live \code{DBI} connection (see \code{\link[=cdm_connect]{cdm_connect()}}).}

\item{cdm_schema}{Schema holding the clinical CDM tables (e.g. \code{"mimic_cdm"}).}

\item{cohort_table}{Cohort table, schema-qualified
(e.g. \code{"results.rwevalidate_test_cohort"}). Must have columns
\code{subject_id}, \code{cohort_definition_id}, \code{cohort_start_date}, \code{cohort_end_date}.}

\item{cohort_id}{Integer cohort definition id to profile.}

\item{obs_window}{Length-2 numeric \code{c(pre, post)} in days relative to index.
The required prior-observation window is \code{abs(obs_window[1])} days
(default 365).}

\item{vocab_schema}{Schema holding the vocabulary tables (\code{concept}). In this
split-schema environment the clinical schema's \code{concept} table is empty, so
the gender label is resolved from \code{vocab_schema}. Default \code{"vocab"}.}

\item{obs_warn_pct, obs_fail_pct}{Prior-observation coverage thresholds (percent
of cohort with sufficient prior observation). Below \code{obs_warn_pct} flags
\code{WARN}, below \code{obs_fail_pct} flags \code{FAIL}. Defaults 70 and 50.}

\item{attrition_warn_threshold}{Reserved for stepwise inclusion-criteria
attrition (single-criterion drop fraction). Not exercised in v0.1 because
criteria lists are not yet a supported input. Default 0.5.}
}
\value{
A named list:
\describe{
\item{cohort_size}{integer - distinct subjects in the cohort}
\item{index_date_summary}{data.frame of \code{index_year}, \code{n}}
\item{obs_coverage}{data.frame: \code{cohort_n}, \code{n_sufficient}, \code{pct_sufficient}, \code{min_prior_days}}
\item{demographics}{data.frame: \code{sex}, \code{mean_age}, \code{min_age}, \code{max_age}, \code{n}}
\item{flags}{character vector of \verb{WARN:}/\verb{FAIL:} messages (empty if all pass)}
}
}
\description{
Profiles an instantiated cohort: size, index-date distribution, prior
observation coverage, and demographics at index. Maps to HARPER section 5 and
RECORD-PE item 6. All thresholds are arguments.
}
