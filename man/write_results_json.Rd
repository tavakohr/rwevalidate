% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/report.R
\name{write_results_json}
\alias{write_results_json}
\title{Write the JSON sidecar (no rendering)}
\usage{
write_results_json(
  results,
  output_dir,
  cdm_schema,
  cohort_id,
  package_version = as.character(utils::packageVersion("rwevalidate")),
  run_date = Sys.time(),
  check_summary = NULL
)
}
\arguments{
\item{results}{Combined results list (e.g. \code{list(attrition = ..., density = ...)},
optionally \code{data_source = ...}).}

\item{output_dir}{Directory to write the report into (created if needed).}

\item{cdm_schema}{CDM schema name (for the report header / JSON).}

\item{cohort_id}{Cohort definition id (for the report header / JSON).}

\item{package_version}{Package version string. Defaults to the installed
\code{rwevalidate} version.}

\item{run_date}{Timestamp for the run. Defaults to \code{\link[base:Sys.time]{base::Sys.time()}}.}

\item{check_summary}{Optional pre-built summary; rebuilt if \code{NULL}.}
}
\value{
The path to the written JSON file (invisibly).
}
\description{
Exposed separately so results can be persisted even when Pandoc is
unavailable for HTML rendering.
}
