% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/module_density.R
\name{run_density}
\alias{run_density}
\title{Module 3 - Temporal Data Density}
\usage{
run_density(
  con,
  cdm_schema,
  cohort_table,
  cohort_id,
  obs_window = c(-365, 365),
  sparse_warn_min = 1
)
}
\arguments{
\item{con}{A live \code{DBI} connection (see \code{\link[=cdm_connect]{cdm_connect()}}).}

\item{cdm_schema}{Schema holding the clinical CDM tables (e.g. \code{"mimic_cdm"}).}

\item{cohort_table}{Cohort table, schema-qualified.}

\item{cohort_id}{Integer cohort definition id.}

\item{obs_window}{Length-2 numeric \code{c(pre, post)} in days relative to index.
Density is computed for records inside \verb{[index + pre, index + post]}.
Default \code{c(-365, 365)}.}

\item{sparse_warn_min}{Minimum total records a domain must have within the
window before it is considered present; domains below this flag \code{WARN}
(likely missing/sparse domain). Default 1 (flag only truly empty domains).}
}
\value{
A named list:
\describe{
\item{density_by_domain}{data.frame: \code{domain}, \code{months_from_index},
\code{n_patients}, \code{n_records}, \code{records_per_patient}}
\item{followup_summary}{data.frame: one row per \code{censoring_reason} with
\code{n} and \code{median_followup_days} (median over that group)}
\item{followup_detail}{per-subject data.frame: \code{subject_id},
\code{effective_end_date}, \code{followup_days}, \code{censoring_reason}}
\item{flags}{character vector of \verb{WARN:}/\verb{FAIL:} messages}
}
}
\description{
Profiles longitudinal data accrual around the index date: record density per
patient per month across clinical domains, and follow-up completeness with a
censoring-reason breakdown. Maps to FDA RWE Reliability (data accrual).
}
