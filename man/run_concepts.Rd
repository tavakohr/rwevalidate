% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/module_concepts.R
\name{run_concepts}
\alias{run_concepts}
\title{Module 1 - Concept Coverage}
\usage{
run_concepts(
  con,
  cdm_schema,
  cohort_table = NULL,
  cohort_id = NULL,
  concept_ids,
  domain = "condition",
  vocab_schema = "vocab",
  prevalence_warn_pct = 10,
  prevalence_fail_pct = 1,
  unmapped_warn_pct = 20
)
}
\arguments{
\item{con}{A live \code{DBI} connection (see \code{\link[=cdm_connect]{cdm_connect()}}).}

\item{cdm_schema}{Schema holding the clinical CDM tables.}

\item{cohort_table}{Cohort table, schema-qualified. Optional and currently
unused; reserved for cohort-restricted prevalence in a later version.}

\item{cohort_id}{Integer cohort definition id. Optional and reserved, see
\code{cohort_table}.}

\item{concept_ids}{Numeric vector of cohort-defining seed concept id(s). Their
descendants are expanded via \verb{\{vocab_schema\}.concept_ancestor}.}

\item{domain}{Domain the seed concepts live in; one of the names of the
internal domain map (\code{"condition"}, \code{"drug"}, \code{"measurement"},
\code{"procedure"}). Default \code{"condition"}.}

\item{vocab_schema}{Schema holding the vocabulary tables. Default \code{"vocab"}.}

\item{prevalence_warn_pct, prevalence_fail_pct}{Concept-prevalence thresholds
(percent of CDM persons with at least one descendant record). Below warn
flags \code{WARN}, below fail flags \code{FAIL}. Defaults 10 and 1.}

\item{unmapped_warn_pct}{Per-domain unmapped-record threshold (percent mapped
to \code{concept_id = 0}) above which a \code{WARN} is flagged. Default 20.}
}
\value{
A named list:
\describe{
\item{prevalence}{data.frame: \code{n_with}, \code{n_total}, \code{pct}}
\item{ancestor_coverage}{data.frame: \code{total_descendants},
\code{present_descendants}, \code{pct_present}}
\item{mapping_by_domain}{data.frame: \code{domain}, \code{n_records}, \code{n_unmapped},
\code{pct_mapped}}
\item{flags}{character vector of \verb{WARN:}/\verb{FAIL:} messages}
}
}
\description{
Profiles whether the cohort-defining concepts are present, well-mapped, and
richly represented in the CDM. Maps to FDA RWE Relevance (are the data fit for
the question?). This is the first module to read the vocabulary schema.
}
