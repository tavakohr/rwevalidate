% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/module_covariates.R
\name{run_covariates}
\alias{run_covariates}
\title{Module 4 - Covariate Feasibility}
\usage{
run_covariates(
  con,
  cdm_schema,
  cohort_table,
  cohort_id,
  comparator_id,
  vocab_schema = "vocab",
  covariate_window = c(-365, 0),
  top_n = 20,
  smd_warn_threshold = 0.1,
  power_baseline_rate = 0.15,
  power_effect_rate = 0.3,
  power_sig_level = 0.05,
  power_warn_threshold = 0.8
)
}
\arguments{
\item{con}{A live \code{DBI} connection (see \code{\link[=cdm_connect]{cdm_connect()}}).}

\item{cdm_schema}{Schema holding the clinical CDM tables.}

\item{cohort_table}{Cohort table, schema-qualified, containing both arms.}

\item{cohort_id}{Integer cohort definition id of the target arm.}

\item{comparator_id}{Integer cohort definition id of the comparator arm.}

\item{vocab_schema}{Schema holding the vocabulary tables. Default \code{"vocab"}.}

\item{covariate_window}{Length-2 numeric \code{c(pre, post)} days defining the
baseline window relative to index for comorbidity/drug covariates.
Default \code{c(-365, 0)}.}

\item{top_n}{Number of top conditions/drugs to profile. Default 20.}

\item{smd_warn_threshold}{Absolute SMD above which a covariate is flagged
imbalanced. Default 0.1 (standard).}

\item{power_baseline_rate, power_effect_rate}{Assumed comparator and target
event rates for the two-proportion power calculation. Defaults 0.15, 0.30.}

\item{power_sig_level}{Significance level for the power calculation. Default 0.05.}

\item{power_warn_threshold}{Power below this value flags a \code{WARN}. Default 0.80.}
}
\value{
A named list: \code{smd_table}, \code{prevalence_table}, \code{power}, \code{flags}.
}
\description{
Compares a target cohort against a comparator on baseline covariates:
standardized mean differences (SMD love-plot data), a per-arm prevalence table,
and a simple power calculation. Maps to HARPER Sec.4 (comparability). Runs only
when a \code{comparator_id} is supplied.
}
